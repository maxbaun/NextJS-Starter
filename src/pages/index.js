import React from 'react';
import Layout from '../layouts/main';

const Home = () => (
  <Layout>
    <h1 className={['test'].join(' ')}>This is the  Home page </h1>
  </Layout>
);

export default Home;
